extends layout

include selector.pug

block content
	table
		tr
			td
				h3 Выберите астрофотографию
				form(action='/requests/'+rid+'/update',method='POST', enctype='multipart/form-data')
					input(type='file',id='photo',name='photo')
					button(type='submit')='Загрузить'